# 对抗性审查 · 事实核查

独立 Agent 逐条把解析中的数值与原始源码比对。

## 结论
**没有任何编造或错误数值**——每个 token、缓动曲线、渐变、时长、文件名、颜色都可溯源到应用包源码。

## 逐项(节选)
| 声明 | 结论 | 源 |
|---|---|---|
| 品牌青 `#20808D`(浅)/ `#1FB8CD`(深) | ✅ | `53351.css` `--perplexity-brand-highlight-rgb: 32,128,141` / `31,184,205` |
| `--transition-func:cubic-bezier(.19,1,.22,1)` + `400ms` | ✅ | `22316.css` |
| `--minor-animation-time:120ms` | ✅ | `53351.css` |
| 回弹 `(.34,1.29,.64,1)` | ✅ | `22319.css` |
| 参数化弹跳 `(.64, var(--bounce-coefficient,1.75),.61,.77)` | ✅ | `22315.css`(默认值 `22354.js`) |
| 头像 `rotate(-20deg)` / `slide-in 300ms` / label 延迟 100ms / 73px / Berkeley Mono | ✅ | `22315.css` |
| 引导壳 preload comet_intro/sphere/rotating_bubble/ambient + React #root | ✅ | `22299.html` |
| `--perplexity-onboarding-gradient-color:#FBFAF4/#000`、`--glow-color:253,252,249` | ✅ | `22329.css` / `22320.css` |
| Lottie 真实存在(播放器 + 8 动画) | ✅ | `53099.js` `<cr-lottie>`;8 个 JSON `v/fr/layers` 齐全 |
| 内置字体 3 个 ttf | ✅ | `Comet.app/.../Fonts/` |

## 已修正
1. **两处行星渐变最亮停靠点**(`#bcd2ff`、`#fff0c0`)原为作者插值 → 改用真实提取的浅色变体 `#b3cbf7`、`#fbebb8`,现全部源码级精确。
2. `comet_tokens.css` 品牌青**明/暗标签写反**(数值本身正确)→ 已改。

## 需知的溯源细节
- Lottie 播放器证据在 `pak` 的 `53099.js`,不在 `source/`(那里只放引导 UI 层),非造假。
- Lottie 颜色以 0–1 浮点存储,直接 grep HEX 无法命中;经 `float→hex` 解析后匹配真实。
