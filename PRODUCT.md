# Design Teardowns 产品说明

<!-- impeccable:product-schema 1 -->

## Platform

web

## 用户与目的

面向需要理解界面构造的设计师、前端工程师、设计工程师与研究学习者；这是依据仓库内容推定的受众，没有本轮用户调研或使用量数据。

项目将真实网页和原生应用界面的观察整理为可追溯的研究：互动拆解、样式与动效参数、实现证据、复刻指南和事实核查。访客可以进入研究页体验复现，再回到出处判断哪些结论来自实测，哪些属于推断。

本文描述当前产品行为。交付状态与验证记录统一见 [README](README.md#验证与交付)。

## 五站画廊

| 站点        | 研究含义                 | 当前界面表达                               |
| ----------- | ------------------------ | ------------------------------------------ |
| Capture     | 捕获真实页面、素材与状态 | 展示归档 Latrix 样本与研究入口。           |
| Measure     | 读取样式与参数           | 随进度出现网格与套准结构。                 |
| Reconstruct | 依证据重建关键体验       | 延续同一台车，展示测量与准星结构。         |
| Verify      | 核对实测、推断与出处     | 红青参考层靠近，用于表达核对过程。         |
| Archive     | 保存研究并供检索         | 可使用有界馆藏；该入口在其他站点也可访问。 |

五站共用一个临界阻尼运动控制器：原生滚动提供目标，站点导航设定目的站并限速；场景和进度读数消费同一个实际进度。五个等距固定工位沿真实三维轨道布置，124 节输送带与样本台同步位移，全程 44 个世界单位，滚轮随方向正反转，相机同步跟随。场景本身不执行研究捕获、测量、重建或事实核查算法。

Three.js r160 随仓库本地提供。金属门架、轨道、输送带、滚轮、发射器、样本、灯光、阴影、光束与准星属于实时三维物理世界；这里有三维几何、基于物理的材质和射线求交，没有刚体或碰撞动力学仿真。样本固定为 Latrix，馆藏卡片直接导航至研究页。

## 馆藏与数据边界

canonical 是 `teardowns/_gallery/catalogue.js`，当前共 **19** 项。初始 featured 固定为六项，顺序为 `shopify-editions,pear,shopify-editions-spring26,moonshot,comet,latrix`。featured 的全部字段和总数从 canonical 确定性派生；维护命令及数量同步见 [README](README.md#维护馆藏)。

Latrix 的 canonical 分类是 **Web · Digital identity**。其《出处与方法》记录原站关于数字生命与 AI Beings 的自述。EasyCode 的 canonical 分类是 **Web · Learning product**，研究对象是 imlagom.com/projects/easycode/ 的练习、指导与反馈页面；其《设计解构》和《出处与方法》是本轮事实依据。ChatGPT 研究的是 OpenAI overview 页面，Gemini 研究的是归档的签出状态入口，均不扩大到未捕获的应用状态。

初始加载只读 featured；打开 Archive 弹窗或操作原位面板的搜索、分类、排序、分页才按需加载完整 catalogue。仅滚动到 Archive 站不会自动打开弹窗。默认 Curated order 将 featured 六项按指定顺序置前，再接上 catalogue 中剩余项目的原始顺序。因此初始展览与加载完整目录后的默认第一页一致，展开面板不会换掉这六项。

搜索覆盖标题、中文标题、简介、类型与分类。分类选项为 reference、agent、product、independent；排序支持上述 Curated order 和标题字母顺序。每页最多六项，最多显示五个页码按钮，当前无筛选时为 6、6、5 三页。页面长度与结果节点数量不会随全部馆藏直接增长；完整 catalogue 下载、内存占用和客户端筛选成本仍随数据量增长。

同一个 `archive-panel` 在页面原位与原生 dialog 之间移动，保留搜索词、分类、排序与当前页。加载完整目录保持默认展示顺序；主动搜索或切换分类会重新筛选并回到第一页。代码提供忙碌标记、无结果提示、清除筛选、加载失败提示与重试。

## 访问与降级

- 用 Python HTTP 静态服务器预览，无需 npm 安装、构建或应用后端。入口和命令见 [README](README.md)。不能将 `file://` 打开方式当作三维验收路径。
- 主画廊的运行时、字体与图像均使用本地路径；按需加载仍有同源 HTTP 请求。研究详情页的外部依赖需另行核查。
- 宽度超过 1200px 且宽高比不低于 1.25 时使用桌面构图，其余窗口使用紧凑布局。窄屏与低高度窗口通过 Archive 弹窗浏览馆藏；低高度时弹窗内部可滚动。
- 减弱动态偏好关闭 CSS 动画和过渡、导航过渡及鼠标视差，三维位置按五个站点离散更新；保留滚动空间和全部站点。
- Three.js 初始化失败、样本图像加载失败或 WebGL 上下文丢失时，显示静态 Latrix 封面。控制脚本可运行时保留方法、导航和按需馆藏。
- 完全禁用 JavaScript 时，`noscript` 提供 Latrix、ChatGPT、Notion、Shopify Editions 四个直链及外部仓库目录；没有完整本地馆藏列表或搜索。
- 提供语义化链接、按钮、原生 dialog、可见焦点、跳转链接与结果状态播报。

## 研究与授权原则

`measured` 是可追溯的实测，`inferred` 是披露依据和限制的推断。红青套准层仅示意这个区分，不是自动像素差或事实核查引擎。研究描述以捕获时状态为准，保留每份研究的来源和局限，不编造数值、用户量、效果或公司定位。

项目代码的 MIT、原创内容的 CC BY 4.0，以及第三方研究素材的独立权利边界见 [NOTICE](NOTICE)。Three.js 与字体保留各自许可，原研究记录中的素材使用限制继续有效。
