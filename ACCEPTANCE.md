# Beamline 交付与验收记录

日期：2026-09-08。工作分支：`codex/beamline-rebuild-20260908`。

## 范围与状态

本次重建首页的实时 Three.js 场景、运动控制器、五站导航和馆藏界面，更新 README、数据维护及开发说明。原有 17 份研究内容保持原位。

实现与本地验收在隔离工作区完成。以下本地记录与线上发布分别留证：PR 运行数据与浏览器 CI，合并后发布工作流再次验证，再部署 GitHub Pages。实际部署状态与提交见 GitHub Actions 的发布记录。验收时仓库没有待关闭的开放 Issue。

## 视觉与真实操作

以用户批准的 `.impeccable/mocks/beamline-c.png` 为视觉方向，交付可交互的三维构造。样本、光束、输送带与门架使用实际空间关系。图像参考没有作为场景背景贴图，也没有用静态截图替代输送运动。

- 已检查 1586×992、1366×768、3440×900 桌面画面及 390×844 手机画面。
- 1024×1366、768×1024 平板采用独立紧凑布局，馆藏两列三行，标题 12px。
- 844×390、568×320 横屏五站全部可见；馆藏弹窗内部滚动，卡片文字、分页与关闭按钮可达。
- 在当前 Codex 预览中逐个点击五章，再从第五章返回第一章；标题、进度、工位随之切换，输送方向反转。另一个旧 Chrome 预览也已刷新。
- 已修复手机弹窗被原位面板宽度覆盖、短屏文字过小、横屏导航裁切及卡片塌缩。

真实截图与测试数据保存于本地验收产物中，运行测试可重新生成；README 使用交付实现的实际浏览器截图。视觉审查由当前 Agent 与同模型子代理完成，没有调用外部模型。

## 自动化检查

```bash
node tools/build-gallery-featured.mjs --check
node tools/check-gallery.mjs
node tools/test-gallery-maintenance.mjs
python3 tools/generate_index.py --check
node tools/check-beamline.mjs
```

前三个 Node.js 数据工具使用内置模块。浏览器检查需要 Playwright 和 Chromium，可以使用本机已有安装，也可独立安装开发测试依赖：

```bash
npm install --prefix /tmp/beamline-devtest playwright
node /tmp/beamline-devtest/node_modules/playwright/cli.js install chromium
PLAYWRIGHT_ROOT=/tmp/beamline-devtest/node_modules/playwright node tools/check-beamline.mjs
```

这不增加画廊的运行时依赖。预览服务器默认使用端口 4174；通过 `BEAMLINE_URL` 指向其他本地服务器。可用 `PLAYWRIGHT_ROOT` 指定已安装的 Playwright，`CHROME_PATH` 指定浏览器。测试使用独立的无头浏览器，不使用个人浏览器档案。产物写入已忽略的 `artifacts/`。

### 数据与维护

已验证 17 条 canonical 数据与六条 featured 全字段一致，研究入口、封面、出处文档、字体和运行时许可路径存在。添加第 18 条的隔离回归验证生成器自动更新数量，首页除计数数字外逐字节不变。旧 `generate_index.py` 已改为兼容入口，不再用旧模板重建首页。

### 交互与故障恢复

浏览器检查覆盖直接定位五站、原生滚动前后移动、点击单站及跨全程、动画重定向、滚轮/触摸/键盘接管、窗口缩放、减弱动态、弹窗焦点、搜索筛选排序分页、空结果、稳定精选顺序、目录网络失败重试、真实研究链接及图片失败降级。

WebGL 恢复检查使用真正的 `WEBGL_lose_context`：覆盖直接恢复、丢失期间切换窗口尺寸、图片未载入时丢失。验收中发现设备丢失早于 DOM 事件的竞争问题，绘制和缩放现检查真实上下文状态；普通渲染错误不会被该恢复路径掩盖。

重建基线完整检查 **30/30 通过**，浏览器为 Chrome 152.0.7977.76；目录专项检查 **18/18 通过**，包含全部 17 个真实研究链接。曾失败的 WebGL 丢失期间缩放用例修复后连续三次通过，最终全套中的三种真实设备丢失恢复场景也均通过。

| 实测项目                   | 本次结果      |
| -------------------------- | ------------- |
| 原生滚动达到目标变化的 90% | 约 133ms      |
| 原生滚动稳停               | 263–335ms     |
| 点击相邻工位               | 1.05s         |
| 点击跨全程正向 / 反向      | 1.87s / 1.85s |
| 行程回弹或越过目标         | 未出现        |

结果保存于 `artifacts/beamline-final-20260908/results.json`，包含应用源文件 SHA-256、输入事件与逐帧进度数据。具体数值是本机这次运行的结果，不是所有设备上的性能承诺。

## 保留与边界

旧工作区的 61 个已有变更文件经 SHA-256 对照，内容保持不变。本次工作全部位于独立分支和工作区。

本次验证覆盖重建画廊及研究页链接可访问性，没有重新审计全部研究结论或第三方资产权利，这些本地结果不代表线上发布结果。静态代码扫描工具因缺少解析模块而降级为正则扫描，未将其零报告视为完整审查通过。

## 上下边界回弹修复

用户实际使用反馈页面上下会弹动。根元素和 body 原本保留系统默认 overscroll，舞台采用 sticky 定位。现将舞台固定在视口，根元素与 body 关闭弹性回弹，保留原生滚动作为输送目标输入；弹窗使用独立的滚动边界。

新增桌面和手机两项边界回归，覆盖在顶端和底端继续滚动、舞台与顶部栏位置不变、边界后五章切换及弹窗正常。修复后的完整检查 **32/32 通过**，报告为 `artifacts/beamline-boundary-fix-20260908/results.json`。Codex 当前预览已刷新，实际 DOM 确认为 fixed 定位及上下 overscroll 禁用。此前重建的时长测量保留为基线，未用于声称原生触控板弹性效果的量化测试。

## 工位与首尾停靠校准

此前停靠位置与门架使用两套坐标，样本实际越过对应门架中心 6.215 个世界单位。现直接从门架的挤出几何中心生成停靠点，取消独立停靠坐标表；相机同步校准构图。首站 z 为 2.715，末站为 -41.285，总行程仍为 44。

新增桌面、平板和手机三项回归：逐个点击五站并回到首站，比较样本背板与真实门架中心的世界坐标；另验证原生滚动到最顶部和最底部时重合，误差小于 1e-7。已检查桌面末站及手机首尾截图。完整检查 **35/35 通过**，报告与截图位于 `artifacts/beamline-docking-fix-20260908/`。这验证的是章节停靠与首尾位置；两站之间仍保留连续滚动。

## 连续滚动与发布验收

按最新实际体验反馈取消自动吸附：滚动结束后保持当前位置，不因松手、关闭弹窗或输入释放而回拉到工位。只有点击五站导航时执行精确停靠。保留单一临界阻尼控制器、实际到站文字、首尾几何对齐和固定视口；减弱动态时样本与文字保持一致。根入口保留查询参数和阶段锚点。

回归检查包含多个两站之间的位置在释放后持续保持、反向输入、点击停靠、原生输入接管、五站与首尾对齐、弹窗、响应式及 WebGL 丢失恢复。报告记录源文件哈希，测试期间源码变更会令检查失败。PR CI 分三组运行同一套检查并保存 `beamline-acceptance-*` 产物；GitHub Pages 发布依赖验证通过。实际发布状态及提交见 [Actions](https://github.com/YunyueLi/design-teardowns/actions/workflows/pages.yml)。

首次 Ubuntu CI 的全质量软件绘制导致数秒级响应，未作为通过结果发布。后续通过真实 SwiftShader 后端识别启用软件渲染降级，硬件后端继续执行原有运动时长阈值。软件渲染的墙钟耗时单独记录，明确标为不能验收硬件 GPU 性能；几何、输入、方向、终点、无回拉与故障恢复等功能断言仍执行。硬件与软件后端分别留证；软件模式降低画面细节预算，几何、导航、输入与故障恢复仍使用相同实现。
